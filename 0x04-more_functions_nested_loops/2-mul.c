#include "holberton.h"

/**
* mul - function that performs a multiplication between two parameters
* @a: parameter to be multiplied
* @b: parameter to be multiplied
* Return: returns the result of the multiplication
*/
int mul(int a, int b)
{
	return (a * b);
}

