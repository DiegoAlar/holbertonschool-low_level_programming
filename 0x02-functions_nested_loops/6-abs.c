#include "holberton.h"
/**
* _abs - returns the absolute number
* @n: the number to be evaluated
* Return: the absolute value
*/
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}

